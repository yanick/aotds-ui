import React from 'react';

function coordsTransform  () { return '' };

class 

export default ({object}) => 
    <g transform={coordsTransform}>
        <use class="ship_shape"
             href="/svg/ship/generic.svg#ship_generic"
             transform="scale(0.4)" />
    </g>;
